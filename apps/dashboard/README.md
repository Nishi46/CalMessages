# apps/dashboard

Coach/clinic web frontend (04 §2). Not scaffolded yet — this app is built in Sprint 9–10 (P1 — see [05 — Sprint Plan](../../Docs/tally-sprint-plan.md)) via `create-next-app`, once there's an API (Sprint 10's `/api/coach/*` surface) for it to call. The empty directory holds this app's place in the workspace layout from Sprint 0 onward.
